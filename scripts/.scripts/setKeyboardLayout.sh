# Only needs to be run once, but saving it here so I don't have to spend an hour working it out again

sudo localectl set-keymap colemak
sudo localectl --no-convert set-x11-keymap "us(colemak),us" pc105 "" shift:both_capslock,grp:alt_shift_toggle

